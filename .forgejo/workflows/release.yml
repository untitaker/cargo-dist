# This file was autogenerated by dist: https://axodotdev.github.io/cargo-dist
#
# Copyright 2022-2024, axodotdev
# SPDX-License-Identifier: MIT or Apache-2.0
#
# CI that:
#
# * checks for a Git Tag that looks like a release
# * builds artifacts with dist (archives, installers, hashes)
# * uploads those artifacts to temporary workflow zip

name: Release

# This task will run whenever you push a git tag that looks like a version
# like "1.0.0", "v0.1.0-prerelease.1", "my-app/0.1.0", "releases/v1.0.0", etc.
# Various formats will be parsed into a VERSION and an optional PACKAGE_NAME, where
# PACKAGE_NAME must be the name of a Cargo package in your workspace, and VERSION
# must be a Cargo-style SemVer Version (must have at least major.minor.patch).
#
# If PACKAGE_NAME is specified, then the announcement will be for that
# package (erroring out if it doesn't have the given version or isn't dist-able).
#
# If PACKAGE_NAME isn't specified, then the announcement will be for all
# (dist-able) packages in the workspace with that version (this mode is
# intended for workspaces with only one dist-able package, or with all dist-able
# packages versioned/released in lockstep).
#
# If you push multiple tags at once, separate instances of this workflow will
# spin up, creating an independent announcement for each one. However, Forgejo
# will hard limit this to 3 tags per commit, as it will assume more tags is a
# mistake.
#
# If there's a prerelease-style suffix to the version, then the release(s)
# will be marked as a prerelease.
on:
  pull_request:
  push:
    tags:
      - '**[0-9]+.[0-9]+.[0-9]+*'

jobs:
  # Run 'dist plan' (or host) to determine what tasks we need to do
  plan:
    runs-on: "ubuntu-22.04"
    outputs:
      val: ${{ steps.plan.outputs.manifest }}
      tag: ${{ github.ref_name }}
      tag-flag: ${{ format('--tag={0}', github.ref_name) }}
      publishing: ${{ !github.event.repository.fork && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install dist
        shell: bash
        run: "curl --proto '=https' --tlsv1.2 -LsSf https://github.com/axodotdev/cargo-dist/releases/download/v1.0.0-rc.1/cargo-dist-installer.sh | sh"
      - id: plan
        run: |
          dist ${{ github.event.inputs.tag-flag || steps.plan.outputs.tag-flag }} plan --output-format=json > plan-dist-manifest.json
          echo "manifest=$(jq -c "." plan-dist-manifest.json)" >> "$GITHUB_OUTPUT"
          echo "artifacts=" >> "$GITHUB_OUTPUT"
      - name: "Upload dist-manifest.json"
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-plan-dist-manifest
          path: plan-dist-manifest.json

  # Build and packages all the platform-specific things
  "build-local-artifacts":
    needs:
      - plan
    runs-on: "ubuntu-22.04"
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      BUILD_MANIFEST_NAME: target/distrib/${{ join(matrix.targets, '-') }}/dist-manifest.json
    strategy:
      fail-fast: false
      matrix: {"include":[{"dist_args":"--artifacts=local --target=aarch64-apple-darwin --target=aarch64-unknown-linux-gnu --target=aarch64-unknown-linux-musl --target=x86_64-apple-darwin --target=x86_64-pc-windows-msvc --target=x86_64-unknown-linux-gnu --target=x86_64-unknown-linux-musl","host":"x86_64-unknown-linux-gnu","install_dist":{"run":"curl --proto \u0027=https\u0027 --tlsv1.2 -LsSf https://github.com/axodotdev/cargo-dist/releases/download/v1.0.0-rc.1/cargo-dist-installer.sh | sh","shell":"sh"},"runner":"ubuntu-22.04","targets":["aarch64-apple-darwin","aarch64-unknown-linux-gnu","aarch64-unknown-linux-musl","x86_64-apple-darwin","x86_64-pc-windows-msvc","x86_64-unknown-linux-gnu","x86_64-unknown-linux-musl"]}]}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: swatinem/rust-cache@v2
        with:
          key: ${{ join(matrix.targets, '-') }}
      - name: Install dist
        run: |
          {"run":"curl --proto '=https' --tlsv1.2 -LsSf https://github.com/axodotdev/cargo-dist/releases/download/v1.0.0-rc.1/cargo-dist-installer.sh | sh","shell":"sh"}
      # Get the dist-manifest
      - name: Fetch local artifacts
        run: |
          dist build ${{ needs.plan.outputs.tag-flag }} --target=${{ join(matrix.targets, ' --target=') }} --output-format=json > $BUILD_MANIFEST_NAME
      - name: Upload local artifacts
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-"build-local-artifacts"-${{ join(matrix.targets, '-') }}
          path: |
            target/distrib/*/
            !target/distrib/*/dist-manifest.json

  # Build and package all the platform-agnostic(ish) things

  # Publish all the artifacts
